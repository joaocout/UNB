#include <bits/stdc++.h>
using namespace std;

int main () {
	int n, m ,q;
	cin >> n >> m >> q;
	vector< pair<int,vector<int>> > grafo(makepair()n);

	for(int i=0; i<m; i++){
		int a, b;
		cin >> a >> b;
		grafo[a].second.push_back(b);
		grafo[b].first
	}
	for(int i=0; i<q; i++){
		int t, x;
		cin >> t >> x;

	}
}