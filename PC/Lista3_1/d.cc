#include <bits/stdc++.h>
using namespace std;

int main () {
    long long n, k;
    cin >> n >> k;
    long long tab[n];
    for(int i=0; i<n; i++) scanf("%d", &tab[i]);

    int soma=0;
    for(int i=0; i<n; i++) soma += tab[i];
    
    int possivel = 1;
    if(soma<k) possivel=0;
    45jm
    

}